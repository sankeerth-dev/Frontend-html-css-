var str = "hello";
console.log(`repeat = `, str.repeat(4))
// str.endsWith(value)

var str = "Welcome to Javascript"
// checking the given word or character must present at the end of the string
var check = str.endsWith("Script")  // regular expressions is not allowed as parameter
console.log(`check =`, check);